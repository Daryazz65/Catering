<Window x:Class="Catering.View.Windows.AdminWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Catering.View.Windows"
        mc:Ignorable="d"
        Title="Администратор" Height="700" Width="1000" WindowStartupLocation="CenterScreen" WindowState="Maximized">
    <DockPanel LastChildFill="True">
        <Grid DockPanel.Dock="Top" Background="#FFF9F3" Height="60">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center" Margin="8,0">
                <Button Name="BtnCategoryDish" Content="Категории и блюда" Width="200" Margin="0 0 8 0" Click="BtnCategoryDish_Click" />
                <Button Name="BtnUsers" Content="Пользователи" Width="150" Margin="0 0 8 0" Click="BtnUsers_Click" />
                <Button Name="BtnLogout" Content="Выйти" Width="80" Margin="0 0 8 0" Click="BtnLogout_Click" />
            </StackPanel>

            <Image Grid.Column="1" Source="/Resources/лого.png" Width="100" Height="45" Margin="8" VerticalAlignment="Center" HorizontalAlignment="Right" />
        </Grid>

        <Frame Name="MainFrame" NavigationUIVisibility="Hidden" />
    </DockPanel>
</Window>
